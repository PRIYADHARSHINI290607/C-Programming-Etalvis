//Get a number from the user, add 2 to that number,and print the result
#include<stdio.h>
int main()
{
    int a,Result;
    printf("Enter a Number:");
    scanf("%d",&a);
    Result=a+2;
    printf("%d",Result);
    return 0;
}
