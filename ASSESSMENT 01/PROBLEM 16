//Get a two-digit number from the user,make the one's digit 0,then print it
#include<stdio.h>
int main()
{
    int a,Result;
    printf("Enter a number:");
    scanf("%d",&a);
    Result=(a/10)*10;
    printf("%d",Result);
    return 0;
    }
