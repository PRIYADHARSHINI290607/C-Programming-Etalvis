//Get a two-digit number from the user and print the reverse of the number
#include<stdio.h>
int main()
{
    int a,d1,d2,Reverse;
    printf("Enter a number:");
    scanf("%d",&a);
    d2=(a%10);
    d1=(a/10);
    Reverse=(d2*10)+d1;
    printf("%d",Reverse);
    return 0;
}
