// write a loop program to print sum of 6 to 1 ,using if/else only.
#include<stdio.h>
int main()
{
    int a=6,sum=0;
    loop:if(a>=1)
    {
     sum=a+sum;
     a--;
      goto loop;
    }
    else
      printf("%d",sum);
    
      return 0;
}
